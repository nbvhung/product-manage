mixin select-tree(items, level = 1, parent_id = "")
    each item in items 
        - const prefix = Array(level + 1).join("-- ") //- hàm Array: tạo ra mảng có (phần tử); join: giá trị các phần tử
        option(
            value=item.id
            selected = (item.id == parent_id ? true : false)
        ) #{prefix}#{item.title}
            if item.children && item.children.length > 0
                +select-tree(item.children, level + 1, parent_id)